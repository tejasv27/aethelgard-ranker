# Aethelgard — Hybrid Intelligence Engine V5 OMEGA
# Dependencies for CPU-only ranking pipeline with LLM + Cross-Encoder + SQLite
# Python >= 3.9 required

# Core ranking engine uses stdlib only: json, gzip, csv, argparse, math, re, datetime, heapq, time, logging, sqlite3

# Metadata validation
pyyaml>=6.0

# Streamlit sandbox UI
streamlit
pandas

# Semantic re-ranking layer (bi-encoder + cross-encoder with sliding window)
sentence-transformers>=2.2.0
torch>=2.0.0

# LLM dynamic weight generation (Gemini 2.5 Flash with structured output)
google-genai>=1.0.0

# System profiling (memory tracking)
psutil>=5.9.0