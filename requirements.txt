# Aethelgard — Hybrid Deterministic + Semantic Intelligence Pipeline
# Dependencies for CPU-only, network-free ranking pipeline
# Python >= 3.9 required

# Core ranking engine uses stdlib only: json, gzip, csv, argparse, math, re, datetime, heapq, time, logging

# Metadata validation
pyyaml>=6.0

# Streamlit sandbox UI
streamlit
pandas

# Semantic re-ranking layer (optional — pipeline falls back gracefully if missing)
sentence-transformers>=2.2.0
torch>=2.0.0